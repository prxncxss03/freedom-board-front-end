import { createContext } from "react";

export const TaskChangeContext = createContext({});

export const CreateModalContext = createContext({});
export const TaskList = createContext({});
export const Focus = createContext({});
export const ColorChange = createContext({});
